# Build output
/.next/
/out/
/build/

# Gradle output from a local dataset pipeline run. Git ignores it via the nested
# .gitignore, but `/build/` above is root-anchored, so without this `format:check`
# fails on a clean tree just because the oracle mod was built locally.
/tools/dataset-pipeline/gtnh-calc-oracle/build/
/tools/dataset-pipeline/gtnh-calc-oracle/.gradle/

# Dependencies and machine-generated lockfiles
/node_modules/
package-lock.json

# Generated / runtime data (mirrors .gitignore)
/public/datasets/
/coverage/
/.pipeline/
/.tmp/
/.gh-artifacts-*/
*.tar

# TypeScript / Next build artifacts
*.tsbuildinfo
next-env.d.ts

# Local-only, never committed (see .git/info/exclude and AGENTS.md git hygiene)
/solver-artifacts-v2/
platline-v4-1.*
CLAUDE.local.md
