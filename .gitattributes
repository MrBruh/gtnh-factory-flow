# Normalize all text files to LF in the repo and on checkout, so Windows
# (core.autocrlf=true) checkouts don't reintroduce CRLF and trip `prettier`
# (endOfLine: lf). Binary files are auto-detected and left untouched.
* text=auto eol=lf

*.sh text eol=lf
